Proyecto Fron con React, para consumir API Clientes.

Al descargar el proyecto ejecutar el comando:
npm install

para iniciar el proyecto ejecutar el comando:
npm run dev

se ejecutara en el URL: http://localhost:5173/

Por inconvenientes con React, solo la pantalla principal de Clientes, logre dejar funcional,
en el backend si funcionan los endpoint.